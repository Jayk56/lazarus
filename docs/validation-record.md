# Infrastructure validation records

Add a record here only after an exact image digest has passed the deployment
gate in [Validation](validation.md). Each record must name the platform,
Kubernetes version, storage configuration, checks performed, and known gaps.
Results apply only to the listed image digest and environment.

The V1 release candidate does not yet have a completed infrastructure record.
